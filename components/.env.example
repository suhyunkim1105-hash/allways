# Copy this file to .env.local for local development
# (Vercel: set the same key under Project Settings -> Environment Variables)

# Google Maps JavaScript API key — required by app/map/page.tsx and
# components/PlaceMap.tsx (Place Detail Home's single-place map).
# Get one at https://console.cloud.google.com/google/maps-apis
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
